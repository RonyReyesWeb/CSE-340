<div class="form-container">
  <%- messages() %>

  <% if (locals.message && message.length > 0) { %>
    <p class="flash-message"><%= message %></p>
  <% } %>

  <%- vehicleDetailHTML %>

  <p>
    <a href="/inquiry/<%= vehicle.inv_id %>">Contact Us About This Vehicle</a>
  </p>
</div>